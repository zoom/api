## Retrieve meeting detail

Retrieve live or past meetings detail

> Definition

```shell
GET https://api.zoom.us/v2/metrics/meetings/{meetingId}
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/metrics/meetings/{meetingId}
```

> Example Response

```text
200 OK
```

```json
{
    "uuid": "string [uuid]",
    "id": "integer",
    "topic": "string",
    "host": "string",
    "email": "string",
    "user_type": "string",
    "start_time": "string [date-time]",
    "end_time": "string [date-time]",
    "duration": "string",
    "participants": "integer",
    "has_pstn": "boolean",
    "has_voip": "boolean",
    "has_3rd_party_audio": "boolean",
    "has_video": "boolean",
    "has_screen_share": "boolean",
    "has_recording": "boolean",
    "has_sip": "boolean"
}
```

Path Arguments | &nbsp;
--- | ---
meetingId **required** | The meeting ID or meeting UUID. If given meeting ID, will take the last meeting instance.

Query Arguments | &nbsp;
--- | ---
type *optional*<div class="default">live</div> | The meeting type<div class="enum">`past` past meeting<br/>`pastOne` past one user meeting<br/>`live` live meeting<br/></div>

