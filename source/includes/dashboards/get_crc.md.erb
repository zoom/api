## Retrieve CRC Port Usage

Get CRC Port usage hour by hour for a specified time period <aside class='notice'>We will report a maximum of one month. For example, if "from" is set to "2015-08-05" and "to" is "2015-10-10" we will adjust "from" to "2015-09-10"</aside>

> Definition

```shell
GET https://api.zoom.us/v2/metrics/crc
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/metrics/crc
```

> Example Response

```text
200 OK
```

```json
{
    "from": "string",
    "to": "string",
    "crc_ports_usage": [
        {
            "date_time": "string",
            "crc_ports_hour_usage": [
                {
                    "hour": "string",
                    "max_usage": "integer",
                    "total_usage": "integer"
                }
            ]
        }
    ]
}
```

Query Arguments | &nbsp;
--- | ---
from | Start Date
to | End Date

