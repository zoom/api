## Retrieve sharing/recording details of webinar participant

Retrieve sharing/recording details of live or past webinar participant

> Definition

```shell
GET https://api.zoom.us/v2/metrics/webinars/{webinarId}/participants/{participantId}/sharing
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/metrics/webinars/{webinarId}/participants/{participantId}/sharing
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "next_page_token": "string",
    "participants": [
        {
            "id": "string",
            "user_id": "string",
            "user_name": "string",
            "details": [
                {
                    "content": "string",
                    "start_time": "string",
                    "end_time": "string"
                }
            ]
        }
    ]
}
```

Path Arguments | &nbsp;
--- | ---
webinarId **required** | The webinar ID or webinar UUID. If given webinar ID, will take the last webinar instance.

Query Arguments | &nbsp;
--- | ---
type *optional* | The webinar type<div class="enum">`past` past webinar<br/>`live` live webinar<br/></div>
page_size *optional* | Number of items returned per page. *Default: 30* *Max: 300*
next_page_token *optional* | Used to paginate through large result sets. A next page token will be returned whenever the set of available participants list exceeds 300. The expiration period is 15 minutes.


