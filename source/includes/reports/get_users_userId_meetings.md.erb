## Retrieve meetings report

Retrieve ended meetings report for a specified period

> Definition

```shell
GET https://api.zoom.us/v2/report/users/{userId}/meetings
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/report/users/{userId}/meetings
```

> Example Response

```text
200 OK
```

```json
{
  "from": "string",
  "to": "string",
  "page_count": "integer",
  "page_size": "integer",
  "total_records": "integer",
  "next_page_token": "string",
  "meetings": [
    {
      "uuid": "string",
      "id": "long",
      "type": "integer",
      "topic": "string",
      "user_name": "string",
      "user_email": "string",
      "start_time": "string",
      "end_time": "string",
      "duration": "integer",
      "total_minutes": "integer",
      "participants_count": "integer"
    }
  ]
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID

Query Arguments | &nbsp;
--- | ---
from **required** | Start Date
to **required** | End Date
page_size *optional* | Number of items returned per page. *Default: 30* *Max: 300*
next_page_token *optional* | Used to paginate through large result sets. A next page token will be returned whenever the set of available meeting list exceeds 300. The expiration period is 15 minutes.

