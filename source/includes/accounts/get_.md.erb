## List accounts

List the sub accounts under the master account

> Definition

```shell
GET https://api.zoom.us/v2/accounts
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/accounts
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_number": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "accounts": [
        {
            "id": "string",
            "owner_email": "string",
            "created_at": "string"
        }
    ]
}
```

Query Arguments | &nbsp;
--- | ---
page_size *optional*    | Number of items returned per page. *Default: 30* *Max: 300*
page_number *optional*  | Page number of results returned. *Default: 1*

