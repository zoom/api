## List a webinar's panelists

List panelists for a webinar

> Definition

```shell
GET https://api.zoom.us/v2/webinars/{webinarId}/panelists
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/webinars/{webinarId}/panelists
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_number": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "registrants": [
        {
            "email": "string",
            "name": "string"
        }
    ]
}
```

Path Arguments | &nbsp;
--- | ---
webinarId **required** | The webinar ID

Query Arguments | &nbsp;
--- | ---
occurrence_id *optional* | The meeting occurrence ID
page_size *optional*<div class="default">30</div><div class="maximum">300</div> | The amount of records returns within a single API call. 
page_number *optional*<div class="default">1</div> | Current page number of returned records.

