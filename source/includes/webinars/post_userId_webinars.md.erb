## Create a webinar

Create a webinars for a user

> Definition

```shell
POST https://api.zoom.us/v2/users/{userId}/webinars
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/webinars \
   -d '{
    "topic": "string",
    "type": "integer",
    "start_time": "string",
    "duration": "integer",
    "timezone": "string",
    "password": "string",
    "agenda": "string",
    "recurrence": {
        "type": "integer",
        "repeat_interval": "integer",
        "weekly_days": "integer",
        "monthly_day": "integer",
        "monthly_week": "integer",
        "monthly_week_day": "integer",
        "end_times": "integer",
        "end_date_time": "string"
    },
     "settings": {
        "host_video": "boolean",
        "panelists_video": "boolean",
        "enforce_login": "boolean",
        "question_answer": "boolean",
        "hd_video": "boolean",
        "practice_session": "boolean",
        "approval_type": "integer",
        "registration_type": "integer",
        "audio": "string",
        "auto_recording": "string",
        "enforce_login_domains": "string",
        "alternative_hosts": "string",
        "close_registration": "boolean",
        "show_share_button": "boolean",
        "allow_multiple_devices": "boolean"
    }
}'
```

> Example Response

```text
200 OK
```

```json
{
    "id": "string",
    "updated_at": "string"
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID

Body Arguments | &nbsp;
--- | ---
topic *optional* | Meeting topic. Max of 200 characters.
type *optional* | Meeting Type<div class="enum">`1` Instant Meeting<br/>`2` Scheduled Meeting<br/>`3` Recurring Meeting with no fixed time<br/>`8` Recurring Meeting with fixed time<br/></div>
start_time *optional* | Meeting start time，in the format "yyyy-MM-dd'T'HH:mm:ss'Z'", should be GMT time. In the format "yyyy-MM-dd'T'HH:mm:ss", should be local time, need to specify the time zone. Only used for scheduled meeting and recurring meeting with fixed time.
duration *optional* | Meeting duration (minutes). Used for scheduled meeting only.
timezone *optional* | Timezone to format start_time, like "America/Los_Angeles".
password *optional* | Meeting password. Password may only contain the following characters: [a-z A-Z 0-9 @ - _ *]. Max of 10 characters.
agenda *optional* | Meeting description. Max of 2000 characters
recurrence.type *optional* | Recurrence meeting type<div class="enum">`1` Daily<br/>`2` Weekly<br/>`3` Monthly<br/></div>
recurrence.repeat_interval *optional* | Recurrence meeting repeat interval. For a Daily Meeting, max of 90. For a Weekly Meeting, max of 12. For a Monthly Meeting, max of 3.
recurrence.weekly_days *optional* | Recurrence Meeting Occurs on week days, multiple value separated by comma<div class="enum">`1` Sunday<br/>`2` Monday<br/>`3` Tuesday<br/>`4` Wednesday<br/>`5` Thursday<br/>`6` Friday<br/>`7` Saturday<br/></div>
recurrence.monthly_day *optional* | Recurrence Meeting Occurs on a month day. The value range is from 1 to 31
recurrence.monthly_week *optional* | Recurrence Meeting Occurs on the week of a month.<div class="enum">`-1` Last week<br/>`1` First week<br/>`2` Second week<br/>`3` Third week<br/>`4` Fourth week<br/></div>
recurrence.monthly_week_day *optional* | Recurrence Meeting Occurs on the week day of a month<div class="enum">`1` Sunday<br/>`2` Monday<br/>`3` Tuesday<br/>`4` Wednesday<br/>`5` Thursday<br/>`6` Friday<br/>`7` Saturday<br/></div>
recurrence.end_times *optional* | Recurrence Meeting End occurrences times
recurrence.end_date_time *optional* | Recurrence Meeting End Date. Should be UTC time, such as 2017-11-25T12:00:00Z.
settings.host_video *optional* | Start video when host join meeting
settings.panelists_video *optional* | Start video when panelists join Webinar.
settings.question_answer *optional* | Enable Q&A
settings.hd_video *optional* | Enable HD video for view-only users.
settings.practice_session *optional* | Enable Practice Session.
settings.approval_type *optional* | <div class="enum">`0` Automatically Approve<br/>`1` Manually Approve<br/>`2` No Registration Required<br/></div>
settings.registration_type *optional* | Registration type. Used for recurring meeting with fixed time only.<div class="enum">`1` Attendees register once and can attend any of the occurrences<br/>`2` Attendees need to register for each occurrence to attend<br/>`3` Attendees register once and can choose one or more occurrences to attend<br/></div>
settings.audio *optional* | Meeting audio options<div class="enum">`both` Both Telephony and VoIP<br/>`telephony` Telephony only<br/>`voip` VoIP only<br/></div>
settings.auto_recording *optional* | <div class="enum">`local` Record to local device<br/>`cloud` Record to cloud<br/>`none` No Recording<br/></div>
settings.enforce_login *optional* | Only signed-in users can join this meeting
settings.enforce_login_domains *optional* | Only signed-in users with specified domains can join meetings
settings.alternatice_hosts *optional* | Alternative hosts emails or IDs. Multiple value separated by comma.
settings.close_registration *optional* | Close registration after event date.
settings.show_share_button *optional* | Show social share buttons on registration page.
settings.allow_multiple_devices *optional* | Allow attendees to join from multiple devices.

