## Update a user's TSP account

Update a user's TSP account

> Definition

```shell
PATCH https://api.zoom.us/v2/users/{userId}/tsp/{tspId}
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/tsp/{tspId} \
   -d '{
    "conference_code": "string",
    "leader_pin": "string",
    "dial_in_numbers": {
        "code": "string",
        "number": "string"
    }
}'
```

> Example Response

```text
204 No Content
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID or email address
tspId **required** | TSP ID

Body Arguments | &nbsp;
--- | ---
conference_code **required** | Conference code, length is less than 16.
leader_pin **required** | Leader PIN, length is less than 16.
dial_in_numbers.code **required** | Country Code
dial_in_numbers.number **required** | Dial-in number, length is less than 16.

