## Add a user's TSP config

Add a user's TSP config

> Definition

```shell
POST https://api.zoom.us/v2/users/{userId}/tsp
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/tsp \
   -d '{
    "conference_code": "string",
    "leader_pin": "string",
    "dial_in_numbers": {
        "id": "string",
        "code": "string",
        "number": "string"
    }
}'
```

> Example Response

```text
201 Created
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID

Body Arguments | &nbsp;
--- | ---
conference_code | Conference code, length is less than 16.
leader_pin | Leader PIN, length is less than 16.
dial_in_numbers.id | Country ID
dial_in_numbers.code | Country Code
dial_in_numbers.number | Dial-in number, length is less than 16.

