# TSP

## The TSP object
List of TSP Accounts

```json
{
    "conference_code": "string",
    "leader_pin": "string",
    "dial_in_numbers": [
        {
            "code": "string",
            "number": "string",
            "type": "string"
        }
    ]
}
```

Property | Description
--- | ---
conference_code *string* | Conference code, numeric value, length is less than 16.
leader_pin *string* | Leader PIN, numeric value, length is less than 16.
dial_in_numbers *array* | List of Dial In Numbers
dial_in_numbers[0].code **required**<div class="maxLength">6</div> | Country Code
dial_in_numbers[0].number **required**<div class="maxLength">16</div> | Dial-in number, length is less than 16.
dial_in_numbers[0].type *optional* | Number type<div class="enum">`toll` Toll number<br/>`tollfree` Toll free number<br/></div>
