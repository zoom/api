# TSP

## The TSP object
List of TSP Accounts

```json
{
    "conference_code": "string",
    "leader_pin": "string",
    "media_number": {
        "code": "string",
        "number": "string"
    },
    "dial_in_numbers": [
        {
            "code": "string",
            "number": "string"
        }
    ]
}
```

Property | Description
--- | ---
conference_code *string* | Conference code, numeric value, length is less than 16
leader_pin *string* | Leader PIN, numeric value, length is less than 16
media_number.code *string* | Country Code
media_number.number *string* | Media number, length is less than 16.
dial_in_numbers *array* | List of Dial In Numbers

