## Retrieve a user's token

Retrieve a user's token

> Definition

```shell
GET https://api.zoom.us/v2/users/{userId}/token
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/token
```

> Example Response

```text
200 OK
```

```json
{
    "token": "string"
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID

Query Arguments | &nbsp;
--- | ---
type | User token<div class="enum">`token` used for starting meeting with client SDK<br/>`zpk` used for generating the start meeting url<br/></div>

