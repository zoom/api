## Add a user's assistant

Add an assistant to a user

> Definition

```shell
POST https://api.zoom.us/v2/users/{id}/assistants
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{id}/assistants
'{
  "assistants": [
    {
      "id": "",
      "email": ""
    },
    {
      "id": "",
      "email": ""
    }
  ]
}
'
```
> Example Response

```text
200 OK
```

```json
{
    "id": "string",
    "added_at": "string"
}
```

Path Arguments | &nbsp;
--- | ---
id **required** | The user ID or email address

Body Arguments | &nbsp;
--- | ---
id *optional* | User ID
email *optional* | User email address. Must have id or email, if given id, the email is ignored.

