## Add a user's assistant

Add an assistant to a user

> Definition

```shell
POST https://api.zoom.us/v2/users/{userId}/assistants
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/assistants \
   -d '{
    "assistants": [
        {
            "id": "string",
            "email": "string"
        }
    ]
}'
```

> Example Response

```text
201 Created
```

```json
{
    "id": "string",
    "add_at": "string [date-time]"
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID or email address

Body Arguments | &nbsp;
--- | ---
assistants *optional* | List of User's assistants.

