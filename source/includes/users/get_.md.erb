## List Users

List users on your account

> Definition

```shell
GET https://api.zoom.us/v2/users
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_number": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "users": [
        {
            "id": "string",
            "email": "string",
            "type": "integer",
            "first_name": "string",
            "last_name": "string",
            "created_at": "string",
            "dept": "string",
            "last_client_version": "string",
            "last_login_time": "string",
            "pmi": "integer",
            "timezone": "string",
            "verified": "integer"
        }
    ]
}
```

Query Arguments | &nbsp;
--- | ---
status *optional* | User status<div class="enum">`active` users with active status<br/>`inactive` users with inactive status<br/>`pending` users with pending status<br/></div>
page_size *optional* | Number of items returned per page.*Default:30* *Max:300*
page_number *optional* | Page number of results returned.*Default:1*

