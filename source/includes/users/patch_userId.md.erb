## Update a user

Update a user on your account

> Definition

```shell
PATCH https://api.zoom.us/v2/users/{id}
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{id} \
   -d '{
      "first_name": "string",
      "last_name": "string",
      "type": "integer",
      "pmi": "integer",
      "timezone": "string",
      "dept": "string",
      "vanity_name": "string",
      "host_key": integer,
      "cms_user_id": "string"
}'
```

> Example Response

```text
204 No Content
```

Path Arguments | &nbsp;
--- | ---
id **required** | The user ID or email address

Body Arguments | &nbsp;
--- | ---
first_name *optional* | User’s first name. Character length is less than 64.
last_name *optional* | User’s last name. Character length is less than 64.
type *optional* | User's type<div class="enum">`1` basic<br/>`2` pro<br/>`3` corp<br/></div>
pmi *optional* | Personal Meeting ID, length must be 10.
timezone *optional* | The time zone id for user profile. For this parameter value please refer to the id value in timezone list JSON file.
dept *optional* | Department for user profile, use for report.
vanity_name *optional* | Personal meeting room name.
host_key *optional* | Host Key, should be 6-digit number
cms_user_id *optional* | Kaltura User Id.

