## Create a user

Create a user on your account

> Definition

```shell
POST https://api.zoom.us/v2/users
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users \
   -d '{
    "action": "string",
    "user_info": {
        "email": "string",
        "type": "integer",
        "first_name": "string",
        "last_name": "string",
        "password": "string"
    }
}'
```

> Example Response

```text
201 Created
```

```json
{
    "id": "string",
    "first_name": "string",
    "last_name": "string",
    "email": "string",
    "type": "integer"
}
```

Body Arguments | &nbsp;
--- | ---
action **required** | Specify how to create the new user<div class="enum">`create` User will get an email sent from Zoom. There is a confirmation link in this email. User will then need to click this link to activate their account to the Zoom service. The user can set or change their password in Zoom. <br/>.<br/>`autoCreate` This action is provided for enterprise customer who has a managed domain. This feature is disabled by default because of the security risk involved in creating a user who does not belong to your domain without notifying the user. <br/><br/>`custCreate` This action is provided for API partner only. User created in this way has no password and is not able to log into the Zoom web site or client. <br/><br/>`ssoCreate` This action is provided for enabled "Pre-provisioning SSO User" option. User created in this way has no password. If it is not a basic user, will generate a Personal Vanity URL using user name (no domain) of the provisioning email. If user name or pmi is invalid or occupied, will use random number/random personal vanity URL. <br/><br/></div>
user_info.email **required**<div class="maxLength">128</div> | User's email address
user_info.type **required** | User's type<div class="enum">`1` basic<br/>`2` pro<br/>`3` corp<br/></div>
user_info.first_name *optional*<div class="maxLength">64</div> | User's first name. Cannot contain more than 5 Chinese words.
user_info.last_name *optional*<div class="maxLength">64</div> | User's last name. Cannot contain more than 5 Chinese words.
user_info.password *optional* | User’s password. Only for "autoCreate" action.

