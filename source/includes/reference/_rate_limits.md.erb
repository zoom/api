# Rate Limits

To preserve the integrity and reliability of the platform our APIs have the following rate limits.

## All API Endpoints

You can run a max of ten requests per second (10 requests/second) unless otherwise noted below.

## Billing APIs

You can run a max of one request per second (1 request/second)

Applies to:

- /v2/\*/billing/\*
- /v2/\*/plan/\*

## Report & Metric APIs

You can run a max of one request per second (1 request/second) unless otherwise noted below.

Applies to:

- /v2/metrics/\*
- /v2/report/\*

You can run a max of one request per minute (1 request/minute)

Applies to:

- /v2/metrics/meetings
- /v2/metrics/meetingdetail

In addition, you can run a max of two thousand request per day (2000 requests/day) on the following

Applies to:

- /v2/metrics/meetings
- /v2/metrics/webinars
- /v2/report/\*