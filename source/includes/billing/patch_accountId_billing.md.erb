## Update an account's billing information

Update a sub account's billing information under the master account <aside>Only for the sub account which is paid by master account</aside>

> Definition

```shell
PATCH https://api.zoom.us/v2/accounts/{accountId}/billing
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/accounts/{accountId}/billing \
   -d '{
    "first_name": "string",
    "last_name": "string",
    "email": "string",
    "phone_number": "string",
    "address": "string",
    "apt": "string",
    "city": "string",
    "state": "string",
    "zip": "string",
    "country": "string"
}'
```

> Example Response

```text
204 No Content
```

Path Arguments | &nbsp;
--- | ---
accountId **required** | The account ID

Body Arguments | &nbsp;
--- | ---
first_name **required** | Billing Contact's first name.
last_name **required** | Billing Contact's last name.
email **required** | Billing Contact's email address.
phone_number **required** | Billing Contact's phone number.
address **required** | Billing Contact's address.
apt *optional* | Billing Contact's apartment/suite.
city **required** | Billing Contact's city.
state **required** | Billing Contact's state.
zip **required** | Billing Contact's zip/postal code.
country **required** | Billing Contact's country.

