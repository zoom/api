## List meetings

List meetings for a user

> Definition

```shell
GET https://api.zoom.us/v2/users/{userId}/meetings
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/meetings
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_number": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "meetings": [
        {
            "uuid": "string",
            "id": "string",
            "host_id": "string",
            "topic": "string",
            "type": "integer",
            "start_time": "string",
            "duration": "integer",
            "timezone": "string",
            "created_at": "string",
            "join_url": "string"
        }
    ]
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID

Query Arguments | &nbsp;
--- | ---
type *optional* | The meeting type<div class="enum">`scheduled` all the scheduled meetings<br/>`live` all the live meetings<br/></div>
page_size *optional* | Number of items returned per page.*Default:30* *Max:300*
page_number *optional* | Page number of results returned.*Default:1*

