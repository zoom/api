## List meetings

List meetings for a user

> Definition

```shell
GET https://api.zoom.us/v2/users/{userId}/meetings
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/users/{userId}/meetings
```

> Example Response

```text
200 OK
```

```json
{
    "page_count": "integer",
    "page_number": "integer",
    "page_size": "integer",
    "total_records": "integer",
    "meetings": [
        {
            "uuid": "string",
            "id": "string",
            "host_id": "string",
            "topic": "string",
            "type": "integer",
            "start_time": "string [date-time]",
            "duration": "integer",
            "timezone": "string",
            "created_at": "string [date-time]",
            "join_url": "string"
        }
    ]
}
```

Path Arguments | &nbsp;
--- | ---
userId **required** | The user ID or email address

Query Arguments | &nbsp;
--- | ---
type *optional*<div class="default">live</div> | The meeting type<div class="enum">`scheduled` all the scheduled meetings<br/>`live` all the live meetings<br/>`upcoming` all the upcoming meetings<br/></div>
page_size *optional*<div class="default">30</div><div class="maximum">300</div> | The number of records returned within a single API call
page_number *optional*<div class="default">1</div> | Current page number of returned records

