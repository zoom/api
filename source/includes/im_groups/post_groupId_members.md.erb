## Add IM Group members

Add members to an IM Group under your account

> Definition

```shell
POST https://api.zoom.us/v2/im/roups/{groupId}/members
```

> Example Request

```shell
$ curl https://api.zoom.us/v2/im/groups/{groupId}/members \
   -d '{
    "members": [
      {
        "id": "string",
        "email": "string"
      }
    ]
}'
```

> Example Response

```text
204 No Content
```

Path Arguments | &nbsp;
--- | ---
groupId **required** | The group ID

Body Arguments | &nbsp;
--- | ---
id *optional* | User ID.
email *optional* | User email. If given ID, the email is ignored.

