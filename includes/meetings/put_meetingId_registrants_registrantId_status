<h2 id="update-a-meeting-registrants-status">Update a meeting registrant&rsquo;s status</h2>

<p>Update  a registrants status for a meeting</p>

<blockquote>
<p>Definition</p>
</blockquote>
<pre class="highlight shell tab-shell"><code>PUT https://api.zoom.us/v2/meetings/<span class="o">{</span>meetingId<span class="o">}</span>/registrants/<span class="o">{</span>registrantId<span class="o">}</span>/status
</code></pre>
<blockquote>
<p>Example Request</p>
</blockquote>
<pre class="highlight shell tab-shell"><code><span class="gp">$ </span>curl https://api.zoom.us/v2/meetings/<span class="o">{</span>meetingId<span class="o">}</span>/registrants/<span class="o">{</span>registrantId<span class="o">}</span>/status <span class="se">\</span>
   -d <span class="s1">'{
    "status": "string"
}'</span>
</code></pre>
<blockquote>
<p>Example Response</p>
</blockquote>
<pre class="highlight plaintext"><code>204 No Content
</code></pre>
<table><thead>
<tr>
<th>Path Arguments</th>
<th>&nbsp;</th>
</tr>
</thead><tbody>
<tr>
<td>meetingId <strong>required</strong></td>
<td>The meeting ID</td>
</tr>
<tr>
<td>registrantId <strong>required</strong></td>
<td>The registrant ID</td>
</tr>
</tbody></table>

<table><thead>
<tr>
<th>Body Arguments</th>
<th>&nbsp;</th>
</tr>
</thead><tbody>
<tr>
<td>status</td>
<td><div class="enum"><code class="prettyprint">pending</code> set registrant status to pending<br/><code class="prettyprint">approved</code> set registrant status to approved<br/><code class="prettyprint">denied</code> set registrant status to denied<br/></div></td>
</tr>
</tbody></table>
